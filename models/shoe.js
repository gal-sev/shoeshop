class Shoe {
    constructor(id, name, size, brand, shoeType, laces, price, stock, imgsrc) {
        id = id;
        name = name;
        size = size;
        brand = brand;
        shoeType = shoeType;
        laces = laces;
        price = price;
        stock = stock; // remove later?
        imgsrc = imgsrc;
    }
}